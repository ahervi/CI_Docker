- name: register the runner
  hosts: "gitlab"
  
  tasks:

  - name: ajout du repo gitlab-runner
    shell: curl -L https://packages.gitlab.com/install/repositories/runner/gitlab-ci-multi-runner/script.deb.sh | sudo bash


  - name: install de gitlab-runner
    become: yes
    shell: apt-get install gitlab-ci-multi-runner -y


